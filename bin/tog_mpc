#!/bin/bash
# COVER="$HOME/.cache/music"
# song=$(mpc current -f '%title%')
# album="$(mpc current -f %album%).png"
 mpc toggle
# time=$(mpc status "%percenttime% %state%" | xargs)
# [ -d "$COVER" ] || mkdir "$COVER"
# if [ -f "$COVER/$album" ]
# then
#   notify-send -i "$COVER/$album" -h int:value:"$(echo $time | cut -d' ' -f1)" "$song" "$(echo $time | cut -d' ' -f2)" -t 1900 -r 4
# else
# ffmpeg -i "$HOME/Music/$(mpc current -f %file%)" "$HOME/.cache/music/tmp.png" -y &> /dev/null
#   notify-send -i "$HOME/.cache/music/tmp.png" -h int:value:"$(echo $time | cut -d' ' -f1)" "$song" "$(echo $time | cut -d' ' -f2)" -t 1900 -r 4
# fi
